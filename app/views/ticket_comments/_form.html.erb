<%= form_for(ticket_comment) do |f| %>
  <% if ticket_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_comment.errors.count, "error") %> prohibited this ticket_comment from being saved:</h2>

      <ul>
      <% ticket_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.hidden_field :ticket_id, :value => ticket_id%>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
